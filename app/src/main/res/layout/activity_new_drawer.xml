<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            android:navigationIcon="@drawable/ic_navigation_close"
            android:elevation="@dimen/small_margin"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />

        <FrameLayout
            android:id="@+id/frame_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/toolbar">

        </FrameLayout>

    </RelativeLayout>

    <com.appeaser.sublimenavigationviewlibrary.SublimeNavigationView
        style="@style/NavigationViewStyle"
        android:id="@+id/navigation_view"
        android:layout_width="304dp"
        android:layout_height="match_parent"
        app:snvItemTextColor="@color/colorPrimary"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:snvHeaderLayout="@layout/drawer_header_new"
        app:snvMenu="@menu/drawer_menu"/>
</android.support.v4.widget.DrawerLayout>